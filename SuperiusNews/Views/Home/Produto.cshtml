@model List<Domain.Entities.CWProduto>

@{
    ViewData["Title"] = "Produtos";
    var produtoRows = "";
    foreach (var produto in Model)
    {
    }
}

<h2>@ViewData["Title"]</h2>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Preço</th>
        </tr>
    </thead>
    <tbody>
        @Html.Raw(produtoRows) <!-- Renderiza as linhas do foreach -->
    </tbody>
</table>
